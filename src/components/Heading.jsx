import React from "react";

// creating a component(function) called Heading which will return value of <h1> tag
// save the file in .jsx format
function Heading() {
  return <h1>My Favourite Foods</h1>;
}
// exporting heading component
export default Heading;
